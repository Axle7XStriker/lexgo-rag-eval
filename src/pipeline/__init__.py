"""RAG pipeline building blocks: chunker → embed → store → retrieve → answer → judge.

Modules land as their PRs merge. See CLAUDE.md for the 4-pipeline (P1..P4)
matrix and the eval-loop conventions.
"""
